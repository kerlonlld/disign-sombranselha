rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Permitir leitura e escrita para todos os documentos (para desenvolvimento)
    // IMPORTANTE: Em produção, restrinja estas regras!
    match /{document=**} {
      allow read, write: if true;
    }
  }
}